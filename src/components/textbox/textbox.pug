//-
  in:
    options.title
    options.placeholder
    options.text

mixin textbox(options)
  -
    var title, placeholder, text
    if(typeof(options) == 'object') {
      title = typeof(options.title) != 'undefined' ? options.title : ""
      placeholder = typeof(options.placeholder) != 'undefined' ? options.placeholder : ""
      text = typeof(options.text) != 'undefined' ? options.text : ""
    }

  .textbox
    h3= title
    input(class = 'textbox-input' placeholder = placeholder value = text)
 
  svg(xmlns="http://www.w3.org/2000/svg" version="1.1")
    //text(x="0" y="150" fill="var(--c_dark50)")= "I love SVG"
     
